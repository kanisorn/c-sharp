using Multithread.SimpleCounter;
using Multithread.UI;

using System;
namespace Multithread
{
    class Program
    {
        static void Main(string[] args)
        {
            // 1. Test run SimpleCounter on multiple thread
            //SimpleCounter.SimpleCounter.TestRun();
            //SimpleCounter.SimpleCounter.TestRunWithSleep();

            // 2. Test UI
            //UI.TestUI win = new TestUI();
           // win.ShowDialog();


            Console.ReadLine();
        }
    }
}
